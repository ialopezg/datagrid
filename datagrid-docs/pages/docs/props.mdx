import Head from 'next/head';
import RootPropTable from '../../components/props/RootPropTable';

<Head>
  <title>DataGrid - Props</title>
</Head>

## All Props

> Many of the props that you can pass to `<DataGrid />` are the same as the ones that you can pass to [react-table](https://react-table.tanstack.com/docs/api/useTable).

### Root Props

Here is a list of all the props that you can pass to the root `<DataGrid />`. Some of these props can be large
objects that are detailed further down below in other prop tables, or linked to

```jsx
<DataGrid
//all the props you can pass here
/>
```

<RootPropTable />
